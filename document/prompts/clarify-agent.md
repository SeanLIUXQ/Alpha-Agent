# ClarifyAgent Prompt

当目标页面、数据来源、展示规则和前后端边界都清晰时，把 PM 需求转换为 `RequirementDsl`。

当需求很模糊时，例如“加一个好看的功能”，或者数据来源、排序/过滤规则、权限、前后端归属不清晰时，不要写代码，而是返回阻塞式 `ClarifyQuestion[]`。

对于 MVP 的文章阅读统计链路，如果需求说明文章详情页应在正文下方显示字数和预计阅读时间，则可以接受该需求，并使用现有前端数据源 `Article.body`。
